# Frontend Next.js — biến server-side (KHÔNG có NEXT_PUBLIC_ → không lộ ra browser).
LG_API_BASE=https://legalguard.duckdns.org
# API key chỉ dùng ở BFF (route handlers / server components) — đừng để lộ client.
LG_API_KEY=
